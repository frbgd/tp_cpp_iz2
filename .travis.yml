language: cpp
dist: bionic
sudo: true
compiler: gcc

addons:
  apt:
    packages:
      - cmake
      - cppcheck

before_script:
  - mkdir build && cd build
  - cmake ..
  - make
  - cp ../tests/test_data* .

jobs:
  include:
    - stage: static_analisys
      name: "Static analisys"
      script:
        - cppcheck --inconclusive --enable=all --language=c ../include/* ../src/*.c ../tests/*.c

    - stage: stress_test
      name: "Run stress_test"
      script:
        - ./stress_test
